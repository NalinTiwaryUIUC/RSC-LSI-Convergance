# Dev / CI — always use -r (this is a requirements file, not a package name):
#   pip install -r requirements.txt -r requirements-dev.txt
# Wrong: pip install requirements-dev.txt  (pip will try to download a bogus PyPI package)
pytest>=7.0
shellcheck-py>=0.9.0
